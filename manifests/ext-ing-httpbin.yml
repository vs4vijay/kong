apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ext-bin
  annotations:
    konghq.com/plugins: viz-rate-limit
spec:
  rules:
  - host: ext-bin.com
    http:
      paths:
      - backend:
          serviceName: httpbin
          servicePort: 80